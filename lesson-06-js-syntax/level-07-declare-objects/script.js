// Level 07 — Declare 10 objects with properties
// TODO: Declare and initialize 10 object variables with >=3 properties each.

// Example:
// let userProfile = { name: "Ava", age: 28, isSubscriber: false };

let userProfile = { name: "Donald", age: 28, isSubscriber: false };

let myProfile = { name: "Marcus", age: 38, isSubscriber: true };

let userName = { name: "Kennedy", age: 24, isSubscriber: true };

let firstName = { name: "Wayne", age: 15, isSubscriber: false };

let lastName = { name: "Allen", age: 39, isScriber: true };

export default userProfile;
